{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/bin/fm-arm-pretool-check.sh --claude"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "payload=$(cat); root=${CLAUDE_PROJECT_DIR:-$(pwd -P)}; if [ -f \"$root/AGENTS.md\" ] && [ -f \"$root/bin/fm-turnend-guard.sh\" ]; then printf '%s' \"$payload\" | \"$root/bin/fm-turnend-guard.sh\"; fi"
          }
        ]
      }
    ]
  }
}
